apiVersion: v1
kind: Service
metadata:
  name: {{ include "amiyabot-mcp.fullname" . }}
  labels:
    {{- include "amiyabot-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amiyabot-mcp.selectorLabels" . | nindent 4 }}