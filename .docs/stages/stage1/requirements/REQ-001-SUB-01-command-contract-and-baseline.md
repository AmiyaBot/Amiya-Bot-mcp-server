# STAGE1-RQ-001-SUB-01 统一命令契约与迁移基线

> 用途：冻结 CLI 与 Web 共享的命令契约、输出语义、默认 URL 口径与探活基线，为后续子需求提供稳定边界。

## 文档内容范围、结构与格式约定（必读）

### 1. 本文档负责什么

本文档用于沉淀 `SUB-01` 的全部结构化记忆，只负责本子需求相关的：

1. 共享命令面与请求/响应契约。
2. stdout 输出、错误处理与退出码语义。
3. 默认本地 URL、探活口径与“本地 URL”判定规则。
4. 后续子需求依赖的迁移基线。

本文档不负责：

1. 直接改造 CLI transport。
2. 直接实现本地自启动。
3. 直接移除 Astrbot 资产。

### 2. 本文档固定结构

1. 文档内容范围、结构与格式约定
2. 需求卡片
3. 背景与目标
4. 当前口径
5. 当前状态视图
6. 功能拆解 / 实施拆解
7. 验收标准
8. 测试记录
9. bug / 修复记录
10. 中断与恢复记录
11. 需求拉扯与决策时间线
12. 待确认问题
13. 状态更新

### 3. 本文档记录格式

1. 优先记录共享契约与默认口径，而不是实现细节。
2. 需要被后续子需求复用的结论必须写成明文。
3. 若某条契约尚未冻结，应明确标注为待确认。

## 需求卡片

- 需求编号：`STAGE1-RQ-001-SUB-01`
- 名称：统一命令契约与迁移基线
- 优先级：`P0`
- 状态：`todo`
- 所属里程碑：`M1`
- 父需求：[REQ-001-project-wide-refactor.md](REQ-001-project-wide-refactor.md)

## 背景与目标

- 当前 CLI 直接在本地进程内构建上下文并执行命令，Web 侧只有状态接口，尚未形成统一执行契约。
- 后续的 Web 执行入口、CLI 包装化与本地自启动都依赖这里冻结统一口径。

## 当前口径

1. 本子需求是所有后续执行面改造的前置依赖。
2. 目标是冻结共享命令契约，而不是在这里直接切换入口实现。
3. 需要在本子需求中明确默认 URL、本地 URL 判定规则与探活基线。

## 当前状态视图

- 当前状态：`todo`
- 当前目标：冻结 Web/CLI 共享的命令契约与迁移基线。
- 当前已知进展：父需求已确认 CLI 必须改为 Web 包装层，且需要保留交互式和单次执行两种模式。
- 下一步动作：
  1. 盘点现有命令集合和参数形式。
  2. 明确统一请求/响应模型。
  3. 冻结默认 URL、本地 URL 判定与探活规则。

## 功能拆解 / 实施拆解

1. 盘点当前 CLI 命令面与参数格式。
2. 定义共享执行接口需要承载的请求/响应语义。
3. 定义 stdout/stderr/退出码口径。
4. 冻结默认本地 URL 与本地判定规则。
5. 固定探活接口和探活成功/失败判据。

## 验收标准

1. CLI 与 Web 的共享命令契约形成明文并可被后续子需求直接引用。
2. 默认 URL、本地 URL 判定规则与探活口径形成明文。
3. 单次执行模式的 stdout 输出与退出码语义形成明文。

## 测试记录

> 当前为空。后续按条目追加。

### 测试记录模板

- 时间：
- 场景：
- 操作步骤：
- 预期结果：
- 实际结果：
- 证据：

## bug / 修复记录

> 当前为空。后续按条目追加。

### bug 条目模板

- 编号：`STAGE1-RQ-001-SUB-01-BUG-XXX`
- 标题：
- 影响范围：
- 复现路径：
- 根因：
- 修复方案：
- 回归结果：
- 状态：

## 中断与恢复记录

> 当前为空。后续按条目追加。

### 中断记录模板

- 时间：
- 中断前进度：
- 中断原因：
- 转去处理：
- 恢复时间：
- 恢复后下一步：

## 需求拉扯与决策时间线

### 2026-04-11

1. 将 `SUB-01` 定义为整个执行面重构的前置子需求。
2. 明确本子需求负责冻结契约和基线，不直接实现 transport 切换。

## 待确认问题

1. Web 执行接口采用单一通用执行接口，还是按命令拆分为类型化 REST 接口？
2. 默认本地 URL 的字面值是否固定为 `http://127.0.0.1:9000`？
3. “本地 URL”的判定范围是否固定包含 `localhost`、`127.0.0.1`、`::1`？

## 状态更新

- 2026-04-11：建立子需求文档。