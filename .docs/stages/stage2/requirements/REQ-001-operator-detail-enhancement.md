# STAGE2-RQ-001 干员详情展示重构与档案数据补全

> 用途：作为 stage2 的首个需求文档，承接当前未提交的干员详情展示改造与相关数据补全工作。

## 文档内容范围、结构与格式约定（必读）

### 1. 本文档负责什么

本文档用于沉淀单个需求上的全部结构化记忆，并采用“状态视图 + 时间线附录”的混合写法，只负责本需求相关的：

1. 背景、目标与当前口径。
2. 当前状态、当前方案与下一步动作。
3. 验收标准、测试记录、bug 修复记录。
4. 中断、恢复、需求拉扯与变更时间线。
5. 待确认问题。

本文档不负责：

1. 记录无关需求的讨论。
2. 记录逐字聊天内容。
3. 替代总览文档承担整体项目管理。

### 2. 本文档固定结构

1. 文档内容范围、结构与格式约定
2. 需求卡片
3. 背景与目标
4. 当前口径
5. 当前状态视图
6. 功能拆解 / 实施拆解
7. 验收标准
8. 测试记录
9. bug / 修复记录
10. 中断与恢复记录
11. 需求拉扯与决策时间线
12. 待确认问题
13. 状态更新

### 3. 本文档记录格式

1. 文档前半部分优先回答“现在是什么状态、下一步做什么”。
2. 时间线部分用于记录开发往返、中断、恢复与决策变化。
3. 本需求内 bug 使用唯一编号，建议格式：`STAGE2-RQ-001-BUG-XXX`。
4. 跨需求 bug 优先同步到 [../bugs.md](../bugs.md)。
5. 测试记录至少写明：时间、场景、结果、证据位置。
6. 若某次讨论只形成“待确认”，写入“待确认问题”，不要伪装成已决策。

## 需求卡片

- 需求编号：`STAGE2-RQ-001`
- 名称：干员详情展示重构与档案数据补全
- 优先级：`P0`
- 状态：`in-progress`
- 所属里程碑：`M2`
- 关联总表：[requirements-index.md](../requirements-index.md)

## 背景与目标

- stage1 的执行面重构已经完成，后续新增能力不能继续写回 stage1 父需求。
- 当前未提交改动集中在干员详情页面视觉重做与资料链路补全，已经形成一个可独立规划和验收的工作面。
- 本需求目标是按整合需求方式推进该工作面的实现、验证与收尾。

## 当前口径

1. 本需求正式承接当前未提交的干员详情增强改动，主题为“干员详情展示重构与档案数据补全”。
2. 本需求按一个整合需求推进，不拆成“展示层”和“数据补全”两个独立需求。
3. 当前阶段验收必需覆盖：基础档案、画师、真名、潜能列表、基建技能、`favorKeyFrames`。
4. stage1 已冻结的执行面架构不属于本需求改动范围，CLI/Web 入口与安装口径继续保持稳定。
5. 当前只将图片卡片与详情数据链路计入本需求的已落地进展；CLI 文本 / JSON 输出已由 STAGE2-RQ-002 接管，不再作为本需求待办。
6. 文本链路的后续方向不再是“继续补旧摘要”，而是转由 STAGE2-RQ-002 负责统一结构化结果契约，再生成 Markdown 与 JSON 两种输出形态。

## 当前状态视图

- 当前状态：`in-progress`
- 当前目标：在已实现的主干基础上补齐多样本验证、剩余差异说明与收尾文档。
- 当前已知进展：
  1. 展示层已新增 `operator_info.json.j2` 视口配置，并重做 `operator_info.html.j2` 的整体布局与样式。
  2. 数据链路已补齐 `handbook_info_table` 与 `building_data` 装载，并在 `OperatorImpl` / `operator.py` 中接入基础档案、画师、真名、潜能、基建技能与 `favorKeyFrames`。
  3. 当前已在银灰样例上完成端到端验证：查询命令返回成功，并生成新的详情卡片图片。
  4. STAGE2-RQ-002 已完成统一输出契约实现；CLI 主输出不再依赖旧的 `artifact.txt` 文本模板。
- 下一步动作：
  1. 对更多干员样例做回归验证，确认不同职业/阵营/资料完整度下的卡片渲染稳定性。
  2. 将文本 / JSON 输出契约相关工作移交到 STAGE2-RQ-002，不再在本需求内混写。
  3. 在验证结果收敛后，补齐测试记录、剩余项和完成判据。

## 功能拆解 / 实施拆解

1. 详情展示层：重构 `operator_info` 模板的布局、样式与渲染视口配置。
2. 数据补全层：补齐干员详情页所需的数据装载、模型字段与组装逻辑。
3. 验证与收尾：补充回归验证、文档回写与范围收口。

## 验收标准

1. 干员详情展示层能够稳定输出新的详情卡片布局，并与当前字段结构匹配。
2. 基础档案、画师、真名、潜能列表、基建技能与 `favorKeyFrames` 能够从数据链路稳定产出，并在详情结果中正确呈现。
3. 文档中明确记录图片卡片链路的验证结果，以及已移交到 STAGE2-RQ-002 的输出契约后续项。

## 测试记录

- 时间：2026-04-13
- 场景：银灰详情卡片端到端验证
- 操作步骤：在项目环境中执行 `main.py op 银灰`，确认命令成功返回，并检查生成的 `artifact.png` 与 `artifact.txt`。
- 预期结果：命令执行成功；生成新的详情卡片图片；图片中可见本阶段纳入验收的字段与技能面板。
- 实际结果：符合预期。命令返回成功，生成了新的详情卡片图片；图片中可见潜能提升、真名、CV、基建技能、技能面板等内容。文本摘要仍为旧版概要输出。
- 证据：`resources/cache/cards/operator_info/operator:银灰:66b2145/artifact.png` 与 `resources/cache/cards/operator_info/operator:银灰:66b2145/artifact.txt`。

### 测试记录模板

- 时间：
- 场景：
- 操作步骤：
- 预期结果：
- 实际结果：
- 证据：

## bug / 修复记录

> 当前为空。后续按条目追加。

### bug 条目模板

- 编号：`STAGE2-RQ-001-BUG-XXX`
- 标题：
- 影响范围：
- 复现路径：
- 根因：
- 修复方案：
- 回归结果：
- 状态：

## 中断与恢复记录

> 当前为空。若开发本需求时被其他 bug 或需求打断，在此记录；跨需求 bug 同步登记到 [../bugs.md](../bugs.md)。

### 中断记录模板

- 时间：
- 中断前进度：
- 中断原因：
- 转去处理：
- 恢复时间：
- 恢复后下一步：

## 需求拉扯与决策时间线

### 2026-04-13

1. 用户明确要求开启 stage2。
2. 基于 stage1 已完成状态和当前未提交改动，stage2 初始化为“干员详情展示重构与档案数据补全”。
3. 用户确认 stage2 正式按该主题推进，首个需求采用整合跟踪方式，并冻结首轮验收字段。
4. 根据当前未提交改动的实际运行结果回写进度：银灰样例已完成图片卡片验证，文本摘要链路仍保持旧版概要输出。

### 2026-04-14

1. 用户提出新的输出方向：CLI 增加 `--json`，JSON 返回与 MCP 对齐的结构化结果；默认 CLI 不再坚持旧摘要，而是输出 Markdown 文本。
2. 经过澄清，确定不把 JSON 设计成 raw model，也不复用现有 PNG 渲染配置模板，而是单独建立语义化结果契约。
3. 该方向已拆出为 STAGE2-RQ-002，避免继续混写在 REQ-001 中。

## 待确认问题

1. 暂无。首轮范围、推进方式与验收字段已完成确认。

## 状态更新

- 2026-04-13：建立 stage2 首个需求文档，并登记当前未提交的干员详情增强改动作为候选范围。
- 2026-04-13：确认 stage2 主题、整合推进方式与首轮验收字段，需求继续保持 `in-progress`。
- 2026-04-13：根据当前未提交改动的实际运行结果回写需求进度；需求从 `M1` 推进到 `M2`，并记录银灰样例已完成图片卡片验证。
- 2026-04-14：将文本 / JSON 输出契约相关后续工作拆分到 STAGE2-RQ-002，本需求继续聚焦详情卡片与数据补全验证。
- 2026-04-14：STAGE2-RQ-002 已完成第一轮实现，CLI 主输出不再依赖旧 `artifact.txt`；本需求后续只保留详情卡片与数据补全的多样本验证工作。